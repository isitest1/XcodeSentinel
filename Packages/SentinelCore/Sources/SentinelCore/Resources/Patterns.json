{
  "version": 2,
  "panelHints": {
    "containerRoles": ["AXOpaqueProviderGroup"],
    "identifiers": [],
    "anchorTexts": [],
    "completedTurnMarker": {
      "role": "AXButton",
      "descriptionText": "Report Concern"
    },
    "_note": "Verified against real Xcode AX dump 2026-08-31. AXOpaqueProviderGroup is the Claude chat message container. Report Concern AXButton marks the end of each completed response."
  },
  "patterns": [
    {
      "id": "weekly-limit",
      "outcome": "weeklyLimited",
      "anyOf": [
        "weekly limit",
        "weekly usage limit",
        "limit resets next week",
        "out of weekly usage"
      ],
      "noneOf": ["5-hour", "5 hour"],
      "enabled": true,
      "note": "Must precede session-limit. Wording unverified against Xcode; update after observing a real weekly-limit event."
    },
    {
      "id": "session-limit",
      "outcome": "sessionLimited",
      "anyOf": [
        "usage limit reached",
        "you've hit your usage limit",
        "message limit reached",
        "limit resets",
        "resets at",
        "try again after"
      ],
      "noneOf": ["weekly"],
      "enabled": true,
      "note": "The rolling 5-hour limit. ResetTimeParser extracts the reset time from the matching text."
    },
    {
      "id": "awaiting-approval",
      "outcome": "awaitingApproval",
      "anyOf": [
        "allow this action",
        "approve",
        "permission to run",
        "wants to run",
        "do you want to proceed",
        "grant access"
      ],
      "enabled": true,
      "note": "Notify-only. Never auto-answered (CLAUDE.md 7.2)."
    },
    {
      "id": "awaiting-user-answer",
      "outcome": "awaitingUserAnswer",
      "anyOf": [
        "waiting for your response",
        "which option",
        "please choose",
        "how would you like to proceed"
      ],
      "role": "AXStaticText",
      "enabled": false,
      "note": "Disabled: hard to distinguish from normal chat text. Enable once a reliable marker is identified."
    },
    {
      "id": "awaiting-continue",
      "outcome": "awaitingContinue",
      "anyOf": ["continue generating", "output truncated", "response was cut off", "click continue"],
      "enabled": true,
      "note": "Wording unverified against Xcode. Update after observing a real truncated response."
    },
    {
      "id": "errored",
      "outcome": "errored",
      "anyOf": [
        "an error occurred",
        "something went wrong",
        "request failed",
        "connection error",
        "failed to complete"
      ],
      "enabled": true
    },
    {
      "id": "working",
      "outcome": "working",
      "anyOf": ["thinking", "generating", "running", "working", "esc to interrupt", "stop generating"],
      "enabled": true,
      "note": "Text-based fallback. Primary working detection uses the active-spinner check in DetectionEngine (role:AXProgressIndicator with no identifier). Scanning is limited to the current turn tail so historical Thinking text does not match."
    },
    {
      "id": "idle",
      "outcome": "idle",
      "anyOf": ["ask claude", "send a message", "type a message", "how can i help"],
      "enabled": true,
      "note": "Placeholder text in the chat input — panel is open but no task is running. Primary idle detection uses the completed-boundary check."
    },
    {
      "id": "completed",
      "outcome": "completed",
      "anyOf": ["done", "finished", "completed", "changes applied"],
      "enabled": false,
      "note": "Disabled: primary completion detection uses the completed-boundary (Report Concern button) check in DetectionEngine."
    }
  ]
}
